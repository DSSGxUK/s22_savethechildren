from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='A package to produce a high-resolution multi-dimensional child poverty index.',
    author='DSSG-STC-UNICEF',
    license='MIT',
)
